# The Wormhole packages declare engines >= 20.19. Never let that turn into a
# silent skip (as optionalDependencies did) or a hard install failure: install
# them and let the code decide at runtime whether it can use them.
engine-strict=false
