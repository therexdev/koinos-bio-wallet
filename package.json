{
  "name": "koinos-bio-wallet",
  "version": "0.2.0",
  "description": "Veive-style smart accounts sealed by your own biometrics — one passkey scan deploys a real on-chain account whose only authority is that passkey, verified by the chain itself. Feeless via its own mana sharer.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/wire-format.test.js",
    "keygen": "node tools/keygen.js",
    "infra-keygen": "node tools/infra-keygen.js",
    "infra-deploy": "node tools/infra-deploy.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "koilib": "^9.0.0"
  },
  "license": "MIT"
}
