// Koinos Bio Wallet for Android — a Trusted Web Activity around
// https://wallet.usekoinos.com. See android/README.md.
plugins {
  // API 36 (Android 16) needs AGP 8.11+; 8.5.2 cannot compile against it at
  // all. AGP 8.11 in turn needs Gradle 8.13+ and JDK 17, both set in CI.
  id 'com.android.application' version '8.11.1' apply false
}
